export interface CreatePlace {
    name: string,
    capacity: number,
    placeKind: string,
    city: string,
    multiplicity: string,
    address: string,
    description: string,
    ownerId: string,
    price: number

    
}